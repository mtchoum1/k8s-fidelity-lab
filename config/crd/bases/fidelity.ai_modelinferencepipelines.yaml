apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: modelinferencepipelines.fidelity.ai
spec:
  group: fidelity.ai
  names:
    kind: ModelInferencePipeline
    listKind: ModelInferencePipelineList
    plural: modelinferencepipelines
    shortNames:
    - mip
    singular: modelinferencepipeline
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .spec.gpuMemoryRequirement
      name: GPU Mem
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ModelInferencePipeline is the Schema for the modelinferencepipelines API.
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: ModelInferencePipelineSpec defines the desired state.
            properties:
              gpuMemoryRequirement:
                minLength: 1
                type: string
              image:
                type: string
              modelName:
                minLength: 1
                type: string
              replicas:
                format: int32
                maximum: 1000
                minimum: 0
                type: integer
              sidecarImage:
                type: string
              sidecarLogging:
                type: boolean
            required:
            - gpuMemoryRequirement
            - image
            - modelName
            type: object
          status:
            description: ModelInferencePipelineStatus defines the observed state.
            properties:
              message:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
              readyReplicas:
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
