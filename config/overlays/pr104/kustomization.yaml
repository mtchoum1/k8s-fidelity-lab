apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# INTENTIONAL TIER 6 BUG (ArgoCD): malformed patch path — kustomize build fails.
patches:
  - path: sidecar-patch.yaml
