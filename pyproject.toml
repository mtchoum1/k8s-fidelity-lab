[project]
name = "k8s-fidelity-lab-inference"
version = "0.1.0"
description = "Inference server for the k8s fidelity lab demo workload"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.0",
]

[tool.uv]
package = false

[tool.uv.sources]
