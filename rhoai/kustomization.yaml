apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Tier 5 (rhoai-in-kind): Reference ODH/RHOAI operator dependencies.
# Replace these URLs with your cluster's pinned operator versions.
resources:
  - odh-subscription.yaml
  - kserve-integration.yaml
